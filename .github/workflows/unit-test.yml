---
name: Unit Tests

defaults:
    run:
        shell: bash

on:
    push:
    pull_request:

jobs:
    test:
        name: Run unit tests
        runs-on: ubuntu-24.04
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              with:
                  submodules: true
            - name: Install Node
              uses: actions/setup-node@v4
              with:
                  node-version: '20'
            - name: Install Dependencies
              run: npm ci
            - name: Run tests
              run: npm test
